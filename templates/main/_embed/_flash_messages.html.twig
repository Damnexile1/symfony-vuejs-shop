{% for label, massages in app.flashes(['success', 'warning', 'info']) %}
    {% for message in massages %}
        <div class="alert alert-{{ label }}text-center">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}